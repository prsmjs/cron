services:
  redis:
    image: redis:7-alpine
    container_name: cron-redis
    platform: linux/amd64
    ports:
      - "6379:6379"
    volumes:
      - cron-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  cron-redis-data:
